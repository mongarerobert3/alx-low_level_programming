#include "main.h"

/**
 * @c: tested character
 * main - check the code.
 *_isupper - function that verifies if a  character is uppercase or not
 *
 * Return: returns 1 if it is upercase, 0 if not.
 */
int _isupper(int c)
{
   if((c >= 'A') && (c <= 'Z'))
	   return (1);
	
}

