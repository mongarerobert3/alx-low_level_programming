#include <stdio.h>
#include "variadic_functions.h"

/* main - sum of all parameters
 * return: always 0
 */
 
int sum_them_all(const unsigned int n, ...)
{
	int a, b, c, sum;
	a = 2, 
	b = 3,
	c = 4;
	if n == 0;
	return 0;
	sum = a + b + c;
	printf("%i", sum);
}

