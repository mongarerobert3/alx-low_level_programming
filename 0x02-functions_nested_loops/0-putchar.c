#include <stdio.h>

/* main - prints _putchar, followed by a new line.
 * return - always 0 (success)
 */

int main(void)
{
	printf("_putchar"\n);

	return (0)#include <stdio.h>

/* main - prints _putchar, followed by a new line.
 * return - always 0 (success)
 */

int main(void)
{
        printf("_putchar"\n);

        return (0);
}
}
